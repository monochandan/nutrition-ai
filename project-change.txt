# Image clickijng option :

<View style={{alignItems: "center", flex: 1, paddingTop: 40}}>
                 {/* <Text className="text-7xl font-sans-extrabold text-success">Click a photo</Text> */}

                <View
                        style={{ flex: 1, width: "100%", justifyContent: "center", alignItems:"center" }}
                        contentContainerStyle={{ alignItems: "center" }}                
                >
                    {/* <Text className="text-xl font-bold text-success">Click a photo</Text> */}
                    {selectedImage.map((item: any, index: number) => {
                        return (
                            <Image 
                                key={item}
                                style={{
                                    width: 400,
                                    height: 300,
                                    borderRadius: 20,
                                    // resizeMode: 'contain',
                                     }}
                                source = {{uri : item}}
                            />
                        )
                    })}
                </View>

                <View className="flex-row justify-between" style={{ alignItems:"center", marginBottom: 40,  paddingHorizontal: 20}}>

                    {/* <Text className="list-action-text" style={{marginRight: 20}}> Select Image</Text> 
                    <Text className="list-action-text"> Select Image</Text>  */}

                        {/* <TouchableOpacity
                            onPress={pickImageAsync}
                            style={{marginRight: 20}}
                            className='justify-center 
                                        item-center 
                                        list-action 
                                        flex 
                                        justify-center
                                        h-14 w-42
                                        rounded-3xl 
                                        mt-10 
                                        mb-24'
                        >
                            <Text className="list-action-text items-center"> Select Image</Text> 

                        </TouchableOpacity> */}

                        <TouchableOpacity
                            onPress={clickImageAsync}
                            style={{marginRight: 30}}
                            className='justify-center 
                                        item-center 
                                        list-action 
                                        flex 
                                        justify-center
                                        h-14 w-42
                                        rounded-3xl 
                                        mt-10 
                                        mb-24'
                        >
                            <Text className="list-action-text items-center">Click Photo</Text> 

                        </TouchableOpacity>

                </View>

     </View>  
          